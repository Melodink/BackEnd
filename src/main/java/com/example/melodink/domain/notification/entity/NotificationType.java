package com.example.melodink.domain.notification.entity;

public enum NotificationType {
    FOLLOW,
    JOB_APPLY,JOB_ACCEPT, JOB_REJECT, APPLY_RESULT,
    POST_COMMENT, COMMENT_REPLY, POST_LIKE,
    WORK_LIKE
}
